$kind: http-request
url: "{{baseUrl}}/auth/v1/account/me"
method: GET
auth:
  type: noauth
scripts:
  - type: afterResponse
    code: pm.test('Status 401', () => pm.response.to.have.status(401));
    language: text/javascript
order: 4100

$kind: http-request
url: "{{baseUrl}}/public/v1/codelists/building-type/no-such-code"
method: GET
auth:
  type: noauth
scripts:
  - type: afterResponse
    code: pm.test('Status 404', () => pm.response.to.have.status(404));
    language: text/javascript
order: 9300

$kind: http-request
url: "{{baseUrl}}/evidence/v1/buildings/"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: '{"name": "Unauthorized Building"}'
auth:
  type: noauth
scripts:
  - type: afterResponse
    code: pm.test('Status 401', () => pm.response.to.have.status(401));
    language: text/javascript
order: 1050

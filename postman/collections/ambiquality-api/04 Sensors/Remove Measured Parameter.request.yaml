$kind: http-request
url: "{{baseUrl}}/evidence/buildings/{{buildingId}}/rooms/{{roomId}}/sensors/{{sensorId}}/measured-parameters/voc"
method: PUT
headers:
  Content-Type: application/json
body:
  type: json
  content: '{"validTo": "{{now}}"}'
scripts:
  - type: afterResponse
    code: pm.test('Status 204', () => pm.response.to.have.status(204));
    language: text/javascript
order: 8000
